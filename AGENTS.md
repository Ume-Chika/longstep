# Longstep 開発ルール

このファイルは、Longstepリポジトリで作業するときの共通ルールです。

## 1. 作業の進め方

作業は次のループで進める。

1. 対象のmini_要件書を確認する
2. 今回の作業範囲と完了条件を確認する
3. 小さな単位で実装する
4. ブラウザまたは軽量な検証で確認する
5. 結果と未解決事項を記録する
6. 必要に応じてmini_要件書を更新する
7. 変更をコミットする

要件にない機能を、実装中の判断だけで追加しない。必要になった場合は、先に要件を更新する。

## 2. 要件書の扱い

- MVPや作業単位の詳細は、Vault側のmini_要件書で管理する
- 作業開始時に、対象mini_要件書の目的・範囲・完了条件を確認する
- 実装中に仕様が変わった場合は、コードだけでなくmini_要件書も更新する
- 計画書は全体の方向を管理し、細かな仕様はmini_要件書へ記載する

## 3. Git運用

### ブランチ

- `main`は常に公開可能な状態を保つ
- まとまった機能は`feature/<内容>`で作業する
- 不具合修正は`fix/<内容>`で作業する
- 小さな文書修正や設定変更は`main`へ直接コミットしてよい

### 作業前後

- 作業前に`git status`で未コミット変更を確認する
- 他の変更を勝手に削除・上書きしない
- 作業後に変更内容と差分を確認する
- 無関係なファイルを同じコミットに含めない

### コミット

コミットは1つの目的にまとめ、次の接頭辞を使う。

```text
feat: 新機能
fix: 不具合修正
docs: 文書変更
refactor: 構造整理
test: テスト追加・変更
chore: 設定・依存関係変更
```

例：

```text
feat: 計画一覧を追加
fix: 正本JSONの保存エラーを修正
docs: MVP要件書への参照を追加
```

### 禁止事項

- `main`への未確認の大きな変更
- `--force`を使ったpush
- 変更内容を確認しないままの大量コミット
- ユーザーの変更を消すリセット・強制上書き
- 秘密情報やAPIキーのコミット

## 4. ファイルとデータのルール

- `.env`やAPIキーなどの秘密情報はコミットしない
- `node_modules/`、ビルド成果物、個人設定ファイルはコミットしない
- `prototypes/`は検討用のためGit管理対象外とする
- ユーザーの計画データはサーバーへ送信しない
- 計画の正本はユーザーが選んだLongstep保存先の`<計画ID>.json`とする
- IndexedDBにはフォルダハンドル・表示設定・最後に開いた計画だけを保存する
- LLM APIをサイトへ直接接続しない

## 5. 実装ルール

- MVPでは必要最小限の機能だけ実装する
- 共有JSONは`id`・`name`・`goal`・`nodes`・`meta`だけを持つ最小構成を保つ
- 計画と中間目標には安定したIDを持たせる
- 表示用に計算できる値を、必要以上にJSONへ保存しない
- 正本JSONの読み込み時は不正データで画面が壊れないようにする
- 保存前に`meta.revision`を確認し、Pythonツールの更新を上書きしない
- 保存・読み込み・権限取得の失敗をユーザーに伝える
- Pythonツールは3.12・標準ライブラリだけで実装する
- UIの文言と状態表示は、日本語で一貫させる

## 6. 検証ルール

機能を追加したら、可能な範囲で次を確認する。

- 主要画面が表示される
- コンソールに重大なエラーがない
- 保存先フォルダへのJSON作成・更新・削除ができる
- IndexedDB削除後もフォルダ再選択で計画を復元できる
- Pythonツールの更新が1秒以内に計画マップへ反映される
- 複数計画・複数プロジェクトを混同しない
- 不正な入力で画面が壊れない
- GitHub Pagesのパスでも静的ファイルが読み込める

ブラウザによる手動確認が必要な場合は、確認項目と結果を作業メモまたはIssueに残す。

## 7. 完了の定義

- mini_要件書の完了条件を満たしている
- 変更内容を確認している
- 必要な検証を実施している
- 未解決事項を記録している
- コミットメッセージから変更目的が分かる
